# 01-上线与基础配置

## 文档定位

当前服务已经上线，这份文档维护上线后的基础配置常量、配置规则和当前 P0 执行清单，不再作为上线前 checklist。

当前数据状态（GSC / GA4 / Clarity / 线上抓取的实测值）一律看 [04-收录监控与数据看板](./04-收录监控与数据看板.md)，本文件不保存快照数据。

## 接入清单

| 项 | 值 |
| --- | --- |
| 主域名 | `https://www.stealbrainrotvalues.com/` |
| 环境变量 | `NEXT_PUBLIC_SITE_URL=https://www.stealbrainrotvalues.com` |
| GSC property | `sc-domain:stealbrainrotvalues.com` |
| GSC sitemap | `https://www.stealbrainrotvalues.com/sitemap.xml` |
| GA4 property | `544159633` |
| Clarity project | `xh3k8gwkev` |

## 当前结论

检查日期：`2026-07-07`（数据明细见 [04 的快照](./04-收录监控与数据看板.md#当前数据快照)）

**基础上线完成，线上 canonical、sitemap、robots 已统一为 www。GSC URL Inspection 已确认 5 个核心页面全部 Submitted and indexed，首页 user canonical 也已更新为 www。当前重点从 request indexing 切换为稳定观察搜索信号。**

## 主域名策略

主版本：

`https://www.stealbrainrotvalues.com/`

必须保持：

- 非 www 301 到 www。
- `NEXT_PUBLIC_SITE_URL=https://www.stealbrainrotvalues.com`。
- canonical 输出 www。
- sitemap URL 输出 www。
- robots 指向 www sitemap。
- GSC 提交 www sitemap。

不要出现：

- 页面最终访问是 www，但 canonical 是非 www。
- sitemap 和 canonical 使用不同域名。
- robots 指向另一套 sitemap。

## 当前 P0 清单

这是全套文档中唯一的当前优先级清单，其他文档只引用不复制。

1. 不再重复 request indexing；5 个核心页面已经收录。
2. 每天记录 GSC impressions / clicks / query / page 数据，看 query 是否连续出现。
3. 部署最小 GA4 工具事件后，验证 `item_search_used`、`filter_used`、`related_tool_clicked`、`official_source_clicked` 是否进入 GA4。
4. 继续观察 sitemap indexed 数是否从 0 更新；当前 URL Inspection 已确认 5 页收录，sitemap indexed 统计可视为延迟。
5. 保持首页 title、H1、canonical、robots、sitemap 稳定，不频繁改动。
6. 在满足 [08 扩展决策](./08-复盘节奏与决策规则.md#扩展决策) 前，不发布 `/trading-values/`、`/best-brainrots/` 或 item detail 页面。

## 监控与记录

- 监控节奏和检查清单（每日 / 每周看什么）：见 [08](./08-复盘节奏与决策规则.md)。
- 指标口径和记录表：见 [04](./04-收录监控与数据看板.md)。

如果发现 404、5xx、noindex、canonical 错误，先修复基础配置，不扩页面。

## 完成标准

基础配置真正完成的标准：

- 5 个核心页面都不是 `URL is unknown to Google`。
- 首页和核心页面 canonical 均为 www。
- GSC user canonical 与 Google canonical 一致。
- sitemap submitted 5 个 URL 且无 errors。
- robots sitemap 与主 sitemap 一致。
- GA4 有真实 page_view。
- Clarity 有真实 session。
- 首页和核心工具页可被 Google 抓取。

## 下一步

完成上述 P0 后，进入：

[04-收录监控与数据看板](./04-收录监控与数据看板.md)

只有满足 [08 的扩展决策门槛](./08-复盘节奏与决策规则.md#扩展决策)后，才进入：

[05-关键词矩阵扩展](./05-关键词矩阵扩展.md)

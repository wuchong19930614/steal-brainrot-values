# Steal a Brainrot Values

Lightweight SEO tool site for the `steal a brainrot values` keyword cluster.

## Local development

```bash
npm install
npm run dev
```

Core routes:

- `/`
- `/calculator/`
- `/tier-list/`
- `/rarity-list/`
- `/updates/`
